import UserProfile from "./userProfile";

// Example object shape for reference
const UserLoginResponse = {
  token: "",
  user: UserProfile // use the JS object from userProfile.js
};

export default UserLoginResponse;